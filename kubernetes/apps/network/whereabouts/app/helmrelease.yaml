apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whereabouts
  namespace: network
spec:
  interval: 5m
  chart:
    chart: whereabouts
    version: 0.9.3
    sourceRef:
      kind: HelmRepository
      name: whereabouts-chart
      namespace: network
  values:
    # add chart values here as needed
