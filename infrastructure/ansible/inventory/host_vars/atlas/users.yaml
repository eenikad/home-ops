---
users:
  - name: christoph
    shell: /bin/bash
    comment: The owner of the box
    password: "{{ user_password_peter }}"
    groups: backup,ahinko

  - name: ines
    shell: /bin/bash
    comment: The one and only Ellen
    password: "{{ user_password_ellen }}"
    groups: backup,ahinko

user_groups:
  - name: backup
  - name: ahinko
